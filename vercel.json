{
  "version": 2,
  "builds": [
    { 
      "src": "src/server.ts", 
      "use": "@vercel/node",
      "config": {
        "includeFiles": "public/**,data/**"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/server.ts" }
  ]
}